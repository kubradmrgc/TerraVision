# Mobile API URL overrides (optional)
#
# Physical device on same Wi-Fi as your PC:
#   Set TERRAVISION_API_URL=http://<YOUR_PC_LAN_IP>:5090
#   (inject at build via babel/metro, or edit src/config/env.ts temporarily)
#
# Defaults in src/config/env.ts:
#   Android emulator → http://10.0.2.2:5090
#   iOS simulator    → http://localhost:5090
#
# Web uses NEXT_PUBLIC_API_BASE_URL in clients/web-nextjs/.env.local
